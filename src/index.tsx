import * as ReactDOM from 'react-dom';
import * as React from 'react';

const App = (props) => {
  return (
    <h1>Hello World</h1>
  );
};

ReactDOM.render(
  <App />,
  document.getElementById('app')
);
